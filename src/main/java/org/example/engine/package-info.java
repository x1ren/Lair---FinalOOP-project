/**
 * Low-level run loop, input polling, and spatial collision — kept separate from
 * {@link org.example.ui} so gameplay rules can be exercised without scene graph types.
 */
package org.example.engine;
